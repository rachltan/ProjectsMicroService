# Frontend React + Docker (Nginx)

This repository now includes a multi-stage Docker setup for the React authentication UI as per the provided instructions.

## Files Added
- `Dockerfile.frontend`: Builds the React app using Node 18 Alpine, then serves the production build with Nginx.
- `docker-compose.yml`: Adds a `frontend` service exposing port 3000.

## Build & Run (Frontend Only)
```bash
# Frontend removed

The React frontend has been removed from active use in this repository. Files were kept in history but replaced with placeholders.

If you need to restore the frontend later, consult the git history or retrieve the original files from an earlier commit.
```
